{
  "name": "p4",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "contract",
    "frontend"
  ],
  "scripts": {
    "dev": "yarn workspace frontend next dev",
    "build": "yarn workspace frontend next build",
    "start": "yarn workspace frontend next start",
    "lint": "yarn workspace frontend next lint",
    "hardhat": "yarn workspace contract hardhat",
    "nest:start": "yarn workspace backend nest start --watch",
    "nest:build": "yarn workspace backend nest build",
    "nest:test": "yarn workspace backend jest"
  }
}
